post:
  tags:
    - Matrix
  summary: 'Get matrices from the index using POST.'
  operationId: post_matrices_index
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../../requests/matrix/form.yml
  responses:
    200:
      description: 'Get the matrices from the index.'
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: ../../models/matrix.yml
              meta:
                type: object
    401:
      description: Unauthorized
    403:
      description: Forbidden
