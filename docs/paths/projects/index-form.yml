post:
  tags:
    - Project
  summary: 'Get projects from the index using POST.'
  operationId: post_projects_index
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../../requests/project/form.yml
  responses:
    200:
      description: 'Get the projects from the index.'
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: ../../models/project.yml
              meta:
                type: object
    401:
      description: Unauthorized
    403:
      description: Forbidden
