post:
  tags:
    - Flow
  summary: 'Get flows from the index using POST.'
  operationId: post_flows_index
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../../requests/flow/form.yml
  responses:
    200:
      description: 'Get the flows from the index.'
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: ../../models/flow.yml
              meta:
                type: object
    401:
      description: Unauthorized
    403:
      description: Forbidden
